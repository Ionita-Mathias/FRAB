{
  "name": "@livetrace/signaling",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ephemeral WebRTC signaling edge — Cloudflare Workers + Durable Objects, zero telemetry retention",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo .wrangler"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.2",
    "@cloudflare/workers-types": "^5.20260831.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.127.1"
  }
}
