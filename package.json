{
  "name": "livetracksuunto",
  "private": true,
  "version": "0.1.0",
  "description": "Privacy-first live tracking for Suunto athletes — WebRTC P2P, E2EE, zero retention",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "codegen": "turbo run codegen",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "turbo": "^2.5.5",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
